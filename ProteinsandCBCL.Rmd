---
title: "ProteinsandCBCL"
output: html_document
---

```{r}
protein_data <- t(proteome.d)
#protein_data <- protein_data[order(as.numeric(rownames(protein_data))), ]
```

```{r}
dim(protein_data)
```

```{r}
colnames(phenotype)
```

```{r}
# If the first column in codebook has no name, give it one
colnames(codebook)[1] <- "variable"

# Subset codebook to only keep rows where the first column is in the colnames of proteinsandCBCL
phenotypes_explained <- codebook[codebook$variable %in% colnames(phenotype), ]

```

